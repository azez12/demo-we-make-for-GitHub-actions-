name: my-first-workflow

on: push
jobs:
    first_job:
      runs-on: ubuntu-latest
      steps:
       - name: welcome message
         run: echo "mu first welcome message"
     
       - name: list files
         run: ls

       - name: read files
         run: cat READMEE.md

