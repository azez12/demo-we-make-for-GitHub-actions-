name: my-first-workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      
      - name: Checkout repo
        uses: actions/checkout@v5
      - name: welcome message
        run: echo "my first welcome message"

      - name: list files
        run: ls

      - name: read file
        run: cat README.md
